"""
Paquete reservado para futura extracción del monolito Flask.

La aplicación en ejecución se construye en ``backend/app.py`` (módulo ``app``)
y se expone vía ``wsgi:app`` en la raíz del proyecto.
"""
