# Scripts archivados

Scripts one-off o reemplazados por bootstrap/migraciones en PostgreSQL. **No** los ejecuta el runtime.

| Archivo | Motivo |
|---------|--------|
| `analytics_legacy.py` | Sustituido por `nodeone/modules/analytics/` |
| `create_certificate_tables_legacy.py` | DDL inicial SQLite; schema vía `app.py` + PG |
